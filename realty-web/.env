# React 개발 서버를 0.0.0.0으로 바인딩 (모든 네트워크 인터페이스에서 접속 허용)
HOST=0.0.0.0
PORT=3000

# 백엔드 API 주소 (로컬/외부 자동 감지)
REACT_APP_API_URL=http://localhost:8081

# Watchpack 시스템 파일 에러 방지 (Windows pagefile.sys 접근 에러 해결)
CHOKIDAR_USEPOLLING=false
FAST_REFRESH=true
